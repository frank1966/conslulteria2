// Configura aquí tu proyecto de Supabase (ver config.example.js para instrucciones).
// Mientras dejes estos valores como están, el formulario funciona en "modo demo":
// valida los campos y muestra el mensaje de éxito, pero no guarda nada.

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
